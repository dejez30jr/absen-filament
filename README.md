#di terminal/shell xampp masuk ke folder htdocs dengan(cd htdocs) -> jalankan git clone https://github.com/dejez30jr/absen-filament.git -> code .

#di terminal vs code -> composer install -> set .env -> php artisan migrate -> php artisan db:seed -> php artisan serve

akses web /admin
